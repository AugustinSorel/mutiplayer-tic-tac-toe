import { motion } from "framer-motion";
import styled from "styled-components";

export const SvgIconStyle = styled(motion.svg)`
  width: 24px;
  height: 24px;
  fill: var(--text-color);

  cursor: pointer;
`;
