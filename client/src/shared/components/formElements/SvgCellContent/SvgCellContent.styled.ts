import { motion } from "framer-motion";
import styled from "styled-components";

export const SvgCellContentStyle = styled(motion.svg)`
  height: 50px;
  width: 50px;
  fill: transparent;
  stroke: var(--text-color);
`;

export const PathCellContent = styled(motion.path)``;
