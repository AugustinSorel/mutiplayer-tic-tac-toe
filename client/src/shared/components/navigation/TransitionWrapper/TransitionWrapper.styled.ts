import { motion } from "framer-motion";
import styled from "styled-components";

export const TransitionWrapperContainer = styled(motion.div)`
  display: flex;
  flex-direction: column;
  height: 100vh;
`;
